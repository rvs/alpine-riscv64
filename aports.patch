diff --git a/main/gcc/APKBUILD b/main/gcc/APKBUILD
index b58d970f24..3762cf5092 100644
--- a/main/gcc/APKBUILD
+++ b/main/gcc/APKBUILD
@@ -23,15 +23,15 @@ replaces="libstdc++ binutils"
 
 : "${LANG_CXX:=true}"
 : "${LANG_D:=true}"
-: "${LANG_OBJC:=true}"
+: "${LANG_OBJC:=false}"
 : "${LANG_GO:=true}"
-: "${LANG_FORTRAN:=true}"
-: "${LANG_ADA:=true}"
+: "${LANG_FORTRAN:=false}"
+: "${LANG_ADA:=false}"
 
 _libgomp=true
 _libgcc=true
 _libatomic=true
-_libitm=true
+_libitm=false
 
 if [ "$CHOST" != "$CTARGET" ]; then
 	if [ "$BOOTSTRAP" = nolibc ]; then
diff --git a/main/pkgconf/APKBUILD b/main/pkgconf/APKBUILD
index 4f39b28d4e..a9db575172 100644
--- a/main/pkgconf/APKBUILD
+++ b/main/pkgconf/APKBUILD
@@ -15,7 +15,7 @@ checkdepends="kyua atf"
 build() {
 	./configure \
 		--build=$CBUILD \
-		--host=$CHOST \
+		--host=riscv64-linux-musl \
 		--prefix=/usr \
 		--sysconfdir=/etc \
 		--mandir=/usr/share/man \
diff --git a/main/isl22/APKBUILD b/main/isl22/APKBUILD
index 31ffbb3cf9..67b01d117c 100644
--- a/main/isl22/APKBUILD
+++ b/main/isl22/APKBUILD
@@ -24,7 +24,7 @@ builddir="$srcdir"/isl-$pkgver
 build() {
 	./configure \
 		--build=$CBUILD \
-		--host=$CHOST \
+		--host=riscv64-linux-musl \
 		--prefix=/usr \
 		--mandir=/usr/share/man \
 		--infodir=/usr/share/info \
diff --git a/main/openssl/APKBUILD b/main/openssl/APKBUILD
index fcc4b6fa5d..b8bcc6e6bf 100644
--- a/main/openssl/APKBUILD
+++ b/main/openssl/APKBUILD
@@ -63,6 +63,7 @@ build() {
 		x86)		_target="linux-elf" ;;
 		x86_64)		_target="linux-x86_64"; _optflags="enable-ec_nistp_64_gcc_128" ;;
 		s390x) 		_target="linux64-s390x";;
+		riscv64) 	_target="linux-generic64";;
 		*)		msg "Unable to determine architecture from (CARCH=$CARCH)" ; return 1 ;;
 	esac
 
@@ -74,7 +75,7 @@ build() {
 		--libdir=lib \
 		--openssldir=/etc/ssl \
 		shared no-zlib $_optflags \
-		no-async no-comp no-idea no-mdc2 no-rc5 no-ec2m \
+		no-asm no-threads no-async no-comp no-idea no-mdc2 no-rc5 no-ec2m \
 		no-sm2 no-sm4 no-ssl2 no-ssl3 no-seed \
 		no-weak-ssl-ciphers \
 		$CPPFLAGS $CFLAGS $LDFLAGS -Wa,--noexecstack
 
diff --git a/main/lvm2/APKBUILD b/main/lvm2/APKBUILD
index 6c78d0cd58..b3f231be23 100644
--- a/main/lvm2/APKBUILD
+++ b/main/lvm2/APKBUILD
@@ -11,7 +11,7 @@ license="GPL-2.0-only LGPL-2.1-only"
 # lvm2 will dlopen libs so we need to add explicit depend
 depends="lvm2-libs=$pkgver-r$pkgrel"
 depends_dev="linux-headers"
-makedepends_build="coreutils"
+makedepends_build="coreutils automake"
 makedepends_host="$depends_dev libaio-dev util-linux-dev"
 makedepends="$makedepends_build $makedepends_host"
 source="https://mirrors.kernel.org/sourceware/lvm2/LVM2.$pkgver.tgz
@@ -46,6 +46,7 @@ build() {
 	export ac_cv_func_malloc_0_nonnull=yes
 	export ac_cv_func_realloc_0_nonnull=yes
 
+	cp /usr/share/abuild/config.sub autoconf/
 	./configure \
 		--build=$CBUILD \
 		--host=$CHOST \
diff --git a/main/cunit/APKBUILD b/main/cunit/APKBUILD
index ef44d83068..3ccd298d82 100644
--- a/main/cunit/APKBUILD
+++ b/main/cunit/APKBUILD
@@ -28,6 +28,7 @@ prepare() {
 
 build() {
 	cd "$builddir"
+	cp /usr/share/abuild/config.sub .
 	./configure \
 		--build=$CBUILD \
 		--host=$CHOST \
diff --git a/main/nghttp2/APKBUILD b/main/nghttp2/APKBUILD
index fd1e637302..c60dcb42aa 100644
--- a/main/nghttp2/APKBUILD
+++ b/main/nghttp2/APKBUILD
@@ -50,6 +50,7 @@ build() {
 		--enable-static \
 		--enable-app
 	make
+	printf "#!/bin/sh\ntrue\n" > src/.libs/nghttpx
 }
 
 package() {
diff --git a/scripts/bootstrap.sh b/scripts/bootstrap.sh
index 8f305a4eaf..641237041f 100755
--- a/scripts/bootstrap.sh
+++ b/scripts/bootstrap.sh
@@ -60,7 +60,7 @@ fi
 if [ ! -d "$CBUILDROOT" ]; then
 	msg "Creating sysroot in $CBUILDROOT"
 	mkdir -p "$CBUILDROOT/etc/apk/keys"
-	cp -a /etc/apk/keys/* "$CBUILDROOT/etc/apk/keys"
+	cp -a /etc/apk/keys/* ~/.abuild/*.pub "$CBUILDROOT/etc/apk/keys"
 	${SUDO_APK} add --quiet --initdb --arch $TARGET_ARCH --root $CBUILDROOT
 fi
 
@@ -88,6 +88,7 @@ CTARGET=$TARGET_ARCH BOOTSTRAP=nobase APKBUILD=$(apkbuildname gcc) abuild -r
 
 # Cross build-base
 CTARGET=$TARGET_ARCH BOOTSTRAP=nobase APKBUILD=$(apkbuildname build-base) abuild -r
+ITER=0
 
 msg "Cross building base system"
 
@@ -96,9 +97,10 @@ EXTRADEPENDS_TARGET="libgcc libstdc++ musl-dev"
 
 # ordered cross-build
 for PKG in fortify-headers linux-headers musl libc-dev pkgconf zlib \
-	   openssl ca-certificates libmd libbsd libtls-standalone busybox busybox-initscripts binutils make \
+	   openssl ca-certificates libmd \
+	   gmp mpfr4 mpc1 isl22 cloog libucontext binutils gcc \
+	   libbsd libtls-standalone busybox busybox-initscripts make \
 	   apk-tools file \
-	   gmp mpfr4 mpc1 isl22 cloog libucontext gcc \
 	   openrc alpine-conf alpine-baselayout alpine-keys alpine-base patch build-base \
 	   attr libcap acl fakeroot tar \
 	   lzip abuild ncurses libedit openssh \
@@ -107,10 +109,11 @@ for PKG in fortify-headers linux-headers musl libc-dev pkgconf zlib \
 	   community/go libffi community/ghc \
 	   brotli libev c-ares cunit nghttp2 curl \
 	   pcre libssh2 community/http-parser community/libgit2 \
-	   libxml2 llvm10 community/rust \
+	   libxml2 llvm11 community/rust \
 	   $KERNEL_PKG ; do
 
-	EXTRADEPENDS_TARGET="$EXTRADEPENDS_TARGET" \
+       if [ "$ITER" -eq "${2:-1000}" ]; then exit 0; else ITER=$(( ITER + 1 )); fi 
+	EXTRADEPENDS_TARGET="$EXTRADEPENDS_TARGET"  EXTRADEPENDS_BUILD="libatomic gcc-$TARGET_ARCH g++-$TARGET_ARCH" \
 	CHOST=$TARGET_ARCH BOOTSTRAP=bootimage APKBUILD=$(apkbuildname $PKG) abuild -r
 
 	case "$PKG" in
@@ -118,6 +121,11 @@ for PKG in fortify-headers linux-headers musl libc-dev pkgconf zlib \
 		# Additional implicit dependencies once built
 		EXTRADEPENDS_TARGET="$EXTRADEPENDS_TARGET $PKG"
 		;;
+	gcc)
+		# After gcc full build, we get to have libatomic that a low of packages require
+		# The reason we need gcc as well is because .so is there, not in libatomic
+		EXTRADEPENDS_TARGET="libatomic gcc $EXTRADEPENDS_TARGET"
+		;;
 	build-base)
 		# After build-base, that alone is sufficient dependency in the target
 		EXTRADEPENDS_TARGET="busybox $PKG"
